{
  "name": "Elastic-Beanstalk-Sample-App",
  "version": "0.0.1",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "sonar-scanner": "*"
  },
  "scripts": {
    "start": "node app.js",
    "test": "echo \"No tests configured yet\" && exit 0",
    "lint": "echo \"No lint rules configured yet\" && exit 0",
    "build": "echo \"No build step required for this app\" && exit 0"
  }
}
